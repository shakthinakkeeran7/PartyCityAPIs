package com.example.tests;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({
   
})
public class TestSuite {
    // This class can be left empty.
    // Its only purpose is to hold the above annotations.
}
